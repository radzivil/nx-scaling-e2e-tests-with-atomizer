/**
 * Toggled by `npm run demo:break` / `npm run demo:fix` during the talk.
 * Editing this file changes a hashed input of every `e2e-ci--*` task,
 * which is what makes the Nx cache invalidation visible on stage.
 */
export const DEMO_FLAGS = {
  buggyDiscount: false,
};
